<svg width="512" height="512" viewBox="0 0 512 512" xmlns="http://www.w3.org/2000/svg">
  <!-- Background circle with subtle cyan glow -->
  <circle cx="256" cy="256" r="240" fill="#00BCD4" opacity="0.15"/>
  
  <!-- Single note (behind, lighter cyan) -->
  <g transform="translate(140, 120)">
    <!-- Note stem -->
    <rect x="50" y="50" width="16" height="200" fill="#00BCD4" rx="4"/>
    <!-- Note head (oval) -->
    <ellipse cx="58" cy="260" rx="35" ry="28" fill="#00BCD4"/>
  </g>
  
  <!-- Double note (in front, overlapping, darker cyan) -->
  <g transform="translate(220, 80)">
    <!-- Note stems -->
    <rect x="40" y="80" width="16" height="200" fill="#00838F" rx="4"/>
    <rect x="100" y="80" width="16" height="200" fill="#00838F" rx="4"/>
    <!-- Connecting beam -->
    <rect x="40" y="80" width="76" height="20" fill="#00838F" rx="4"/>
    <!-- Note heads (ovals) -->
    <ellipse cx="48" cy="290" rx="35" ry="28" fill="#00838F"/>
    <ellipse cx="108" cy="290" rx="35" ry="28" fill="#00838F"/>
  </g>
</svg>
